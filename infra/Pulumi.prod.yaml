config:
  # Fill in your Cloudflare Account ID (dashboard right sidebar). Not a secret.
  center-basher-infra:accountId: "REPLACE_WITH_CLOUDFLARE_ACCOUNT_ID"
  center-basher-infra:domain: thecenterisalie.org
  center-basher-infra:githubOwner: jamesvillarrubia
  center-basher-infra:githubRepo: center-basher
  center-basher-infra:productionBranch: claude/dazzling-maxwell-sSHUR
  center-basher-infra:projectName: thecenterisalie
  # The Cloudflare API token is a SECRET — do not put it here in plaintext. Set it with:
  #   pulumi config set --secret cloudflare:apiToken <TOKEN>
  # (Pulumi stores it encrypted in this file.) Or export CLOUDFLARE_API_TOKEN.
